# Core data stack
# Had to bump numpy floor to 2.0. scikit-learn 1.8 is built against numpy 2.x, 
# and mixing it with numpy 1.x silently breaks average_precision_score without throwing an error.
numpy>=2.0,<3.0
pandas>=2.2,<3.0
scipy>=1.13

# Machine learning
scikit-learn>=1.6,<2.0
joblib>=1.4          # transitive sklearn dep — pinned explicitly (used in export.py)
lightgbm>=4.4
xgboost>=3.0

# Storage
pyarrow>=16          # Parquet read/write for aggregate checkpointing

# Visualisation
matplotlib>=3.8
seaborn>=0.13

# Experiment tracking (optional, soft-imported in pipeline)
mlflow>=2.10

# Testing and code quality
pytest>=8.0
pytest-cov>=5.0
ruff>=0.4.0

# Analytics Engineering
dbt-core>=1.8.0
dbt-postgres>=1.8.0